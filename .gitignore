# Debian packaging (generated by bloom-generate + fakeroot debian/rules binary)
debian/

# In-source CMake build directory (used by dpkg-buildpackage)
.obj-*/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# colcon workspace artifacts (if built inside the package directory)
build/
install/
log/
